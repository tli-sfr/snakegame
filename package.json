{
  "name": "ai-native-snake-game",
  "version": "26.2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20",
    "npm": ">=9"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "vite": "4.5.5"
  },
  "devDependencies": {
    "@playwright/test": "1.46.1",
    "typescript": "5.5.4",
    "vitest": "0.34.6"
  }
}
